# does this work?

class Shape(object):

#expirmenting Github with nested files, OOP (from what I learned)
# just clicking on the file each time updates seamlessly.
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def move_by(self, to_x, to_y):
        self.x += to_x
        self.y += to_y

#modify code.